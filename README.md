B1:cài anaconda bản mới nhất \n
B2:khởi tạo project sử dụng anaconda 
B3:chạy các dòng lệnh sau trong terminal
pip install cmake
install -c conda-force diib
install -c conda menpo opencv
pip install face_recognition
B4 chạy lại 
pip install opencv-python numpy deepface face-recognition
chạy file APINhandienkhuonmat
